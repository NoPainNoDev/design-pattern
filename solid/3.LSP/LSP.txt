LSP(Liskov Substitution Principle)
리스코프 치환 원칙

만약 B가 A의 자식클래스라면, A는 어떠한 경고도 내지 않으며 B로 대체가 가능하다.

목표: 자식클래스가 부모클래스의 역할을 똑같이 수행할 수 있게 한다.
이유: 자식클래스가 부모클래스 기능을 똑같이 수행할 수 없을때, 이는 버그를 발생시키는 요인이다.

일관성을 유지한다.
자식, 부모클래스 모두 오류없이 동일한 방식으로 사용할 수 있도록 한다.